# Build-stage only: converting the checkpoint needs transformers + torch, which never
# reach the runtime image. transformers stays on 4.x, which CTranslate2's converter
# targets. torch is installed separately from the CPU-only wheel index.
transformers==4.57.6
ctranslate2==4.8.2
